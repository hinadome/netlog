# Optional operator notes for VM deploys (static SPA — no runtime secrets required).
# Preserved across re-deploys when present at /opt/netlog/.env.production

DOMAIN=netlog.example.com
PUBLIC_URL=https://netlog.example.com
